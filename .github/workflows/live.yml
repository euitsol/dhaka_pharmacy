name: Deploy Application to Live Server

on:
  push:
    branches: ["live"]
  pull_request:
    branches: ["live"]

jobs:
  laravel-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@master
        with:
          host: "82.112.235.17"
          port: "22"
          username: "dhaka6191"
          password: "dhaka6191"
          script: |
            cd home/dhaka6191/dhakapharmacy.com.bd/public_html
            git fetch --all
            git reset --hard origin/live
            git pull
